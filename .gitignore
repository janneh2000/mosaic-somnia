# node
node_modules/
.next/
dist/
*.tsbuildinfo
.npm/
npm-debug.log*

# foundry
contracts/out/
contracts/cache/
broadcast/
# NOTE: contracts/lib/ is intentionally vendored (committed) so a plain
# `git clone` builds with `forge test` and no submodule init step.

# env / secrets
.env
.env.*
!.env.example

# logs
*.log
agents/logs/

# IDE
.vscode/
.idea/
.DS_Store
contracts/out */
contracts/cache */
